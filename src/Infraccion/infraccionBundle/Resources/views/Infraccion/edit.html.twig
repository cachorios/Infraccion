<div id="contenidoEdInfra">
<h2>Edición de Infracción</h2>

<p>
    Tenga en cuenta el cambio de cualquiera de estos datos, salvo observación, implica el renombre de las
    imagenes. Y la infracion podria moverse a otro dia.<br>
    Siendo que este día yo pudo haberse procesdo en su totalidad. y quedarias sin control.
</p>

<form id="{{ idForm }}" class="form-horizontal" action="{{ path('infraccion_update', { 'id': entity.id,'idForm': idForm }) }}"
      method="post" {{ form_enctype(edit_form) }}>
    <input type="hidden" name="_method" value="PUT"/>
    {{ form_widget(edit_form) }}

</form>
<div class="error">{{ error }}</div>
</div>